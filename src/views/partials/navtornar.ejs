<div class="container d-flex justify-content-center align-items-center">
    <a href="#" class="btn btn-secondary btn-sm my-3 w-25" id="tornar">
        <% if(idioma == 0){ %>
            Tornar
        <% } else {%>  
            Volver
        <% } %>
    </a>
</div>
